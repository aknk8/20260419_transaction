# 取引管理システム 将来実装事項バックログ

作成日：2026-05-06  
最終更新：2026-05-07（第2〜6期完了実績を反映）  
作成根拠：現行実装状況（第1期完了）+ `requirements_definition.md` + 各実装計画書のギャップ分析 + チームレビュー

---

## 文書の目的

本書は、現時点で実装済みの機能と要件定義の要件を照合し、**未実装・不完全な実装・将来対応と明示された項目**を網羅的に列挙する。優先度・カテゴリ別に整理することで、次期以降のスプリント計画立案の入力として使用することを目的とする。

---

## チームレビュー結果サマリ（2026-05-06）

初版バックログ作成後、以下4職能による横断レビューを実施した。


| レビュアー      | 視点                  | 主な指摘                                                                                        |
| ---------- | ------------------- | ------------------------------------------------------------------------------------------- |
| プロダクトマネジャー | 業務価値・要件充足・受入条件との照合  | 発注の仕入先別分割・合算請求・顧客マスタの締日管理・受注承認バリデーション等の業務ロジック穴、インフラ必須要件（TLS・バックアップ）の欠落、一部項目の優先度過小評価         |
| テックリード     | 技術アーキテクチャ・非機能要件・保守性 | インフラ基盤（CI/CD・ログ収集・監視・環境分離）の完全欠落、採番競合制御・トランザクション設計・楽観的ロック等の技術的負債、統合テスト層の欠如                   |
| UXデザイナー    | ユーザビリティ・画面品質・操作性    | API接続後に全画面で必要となるフィードバックUI（ローディング・トースト・エラー）の欠落、マスタサジェスト・パンくず・Empty State等の共通UX部品、モバイル対応の完全欠落 |
| QAエンジニア    | 品質保証・異常系・状態遷移・整合性   | 完全業務フローE2E・入金消込異常系・社長決裁境界値・確定済み請求変更ブロック等の重要テスト欠落、CI/CD自動テスト・カバレッジ計測の仕組み未定義                  |


**レビューにより新規追加：** 8〜11章（業務ロジック補完・インフラ・UX基盤・テスト追加）  
**優先度引き上げ：** 4-03（★★→★★★）、4-04（★→★★）、4-05（★→★★）

### 第2回チームレビュー追加項目（2026-05-06）

ユーザーから提示された追加機能要望3件について全職能によるディスカッションを実施。


| 要望                 | 判定                           | 対応                   |
| ------------------ | ---------------------------- | -------------------- |
| ①ダッシュボードのユーザカスタマイズ | バックログ・要件定義ともに未記載             | 3-09として新規追加（☆ 将来対応）  |
| ②部門の追加・削除・名称変更     | 8-06-aに「登録・編集・無効化」として実質カバー済み | 8-06に参照整合性チェックの補足を追記 |
| ③レポートのグラフ表示        | バックログ・要件定義ともに未記載             | 8-14として新規追加（★ 通常）    |


---

## 現状サマリ

> ⚠️ 下表は**初版作成時点（第1期完了後）の状態**を記録したもの。第2〜6期を経て大幅に実装が進んでいる。各項目の現行状態は後続セクションの「対応結果」欄を参照のこと。


| 軸         | 初版作成時の状態（第1期完了後）                             | 2026-05-07時点                                                           |
| --------- | -------------------------------------------- | ---------------------------------------------------------------------- |
| フロントエンド画面 | S-01〜S-14 の画面実装はほぼ完了（S-07・S-14・S-15 は部分的）    | 同左（残留課題は各バックログ項目参照）                                                    |
| データ永続化    | **なし**（インメモリ配列。ページ再読み込みで全データ消滅）              | **PostgreSQL + Drizzle ORM で完全永続化済み**（マイグレーション001〜004適用）               |
| バックエンドAPI | **なし**                                       | **Fastify v5 による全業務エンドポイント実装済み**（E2E556件通過確認）                          |
| 認証        | localStorage セッション＋ソースコードに埋め込まれたユーザ3名（本番非対応） | **JWT + bcrypt + httpOnly Cookie 実装済み**。リフレッシュトークンローテーション・アカウントロック対応済み |
| 認可（サーバー側） | **なし**（認証済みなら権限に関わらず全操作可能）                   | `**authorization.js` ミドルウェア実装済み**（CSRF保護・レートリミット含む）                    |
| 操作ログ・監査証跡 | **なし**                                       | `**audit_logs` テーブル実装済み**（承認・請求確定等の操作を記録）                              |
| 通知ロジック    | 画面のみ。通知生成・配信ロジック未実装                          | 未対応（残バックログ 3-02 参照）                                                    |
| 添付ファイル    | フロントのみ。永続化・アクセス制御なし                          | 未対応（残バックログ 3-05 参照）                                                    |
| 帳票PDF出力   | 見積書・発注書・請求書のHTMLプレビューのみ                      | 未対応（残バックログ 4-01 参照）                                                    |
| 外部システム連携  | **なし**                                       | 未対応（残バックログ 6章 参照）                                                      |


---

## バックログ一覧

優先度凡例：★★★ 緊急 / ★★ 重要 / ★ 通常 / ☆ 将来対応

---

## 1. 基盤整備（本番運用の前提条件）

### 1-01 バックエンドAPI実装 ★★★ ✅ 対応済

**対応要件：** 9.3, 12.3, 12.4  
**現状（初版）：** フロントエンドのみ。データ永続化なし。

> **対応結果（第2〜3期）：** Node.js + Fastify v5 による RESTful API サーバーを構築し、全業務伝票・マスタ管理・承認・通知・レポート API を実装。統一レスポンス形式（`data`/`meta`/`error`）を適用。`app.js` のインメモリ配列を API 呼び出しへ全面置き換え。E2E テスト556件通過により全エンドポイントの動作を確認済み。


| #      | 実装内容                                                                |
| ------ | ------------------------------------------------------------------- |
| 1-01-a | Node.js + Fastify v5 によるRESTful APIサーバー構築                           |
| 1-01-b | 全業務伝票のCRUDエンドポイント実装（案件・見積・受注・発注・納品・請求・入金・支払）                        |
| 1-01-c | マスタ管理APIの実装（顧客・仕入先・商品・ユーザ・部門・税率・支払条件）                               |
| 1-01-d | 承認・通知APIの実装                                                         |
| 1-01-e | レポートAPIの実装（売上集計・未収・未払）                                              |
| 1-01-f | 統一レスポンス形式（成功時 `data` + `meta`、エラー時 `error.code/message/details`）の実装 |
| 1-01-g | フロントエンド（app.js）のインメモリ配列をAPI呼び出しへ段階的に置き換え                            |


---

### 1-02 データベース（永続化）実装 ★★★ ✅ 対応済

**対応要件：** 9.1, 9.2, 9.3  
**現状（初版）：** 永続化なし。

> **対応結果（第2〜3期）：** PostgreSQL + Drizzle ORM によるスキーマ定義・マイグレーション管理を導入。全エンティティのテーブル定義（001_initial.sql）・アカウントロック（002_account_lock.sql）・インデックス（004_indexes.sql）を含む計4マイグレーション適用済み。論理削除・タイムスタンプ自動付与・外部キー参照関係もすべて実装。


| #      | 実装内容                                                                 |
| ------ | -------------------------------------------------------------------- |
| 1-02-a | PostgreSQL のセットアップ（ローカル: Docker Compose、本番: マネージドDB）                 |
| 1-02-b | Drizzle ORM によるスキーマ定義・マイグレーション管理の導入                                  |
| 1-02-c | 全エンティティのテーブル定義（ユーザ・権限・顧客・仕入先・商品・案件・見積・受注・発注・納品・請求・入金・支払・承認履歴・システム設定） |
| 1-02-d | 伝票間の外部キー参照関係の実装                                                      |
| 1-02-e | 論理削除（`status` フィールド管理）の実装                                            |
| 1-02-f | 作成者・更新者・作成日時・更新日時の自動付与                                               |


---

### 1-03 認証基盤の刷新 ★★★ ✅ 対応済

**対応要件：** 10.5, 12.3  
**現状（初版）：** localStorageセッション＋ハードコード済みユーザ3名。

> **対応結果（第2〜4期）：** `@fastify/jwt` による JWT（アクセストークン15分 + リフレッシュトークン）・httpOnly Cookie・bcrypt パスワードハッシュを実装。`/api/auth/refresh-token` エンドポイントでリフレッシュトークンローテーション・盗用検知も実装済み（T-11 / チームC2）。DB 管理によるユーザ追加・変更・無効化が可能。`auth.test.js` 48件全通過確認。


| #      | 実装内容                                                 |
| ------ | ---------------------------------------------------- |
| 1-03-a | @fastify/jwt を使った JWT（アクセストークン15分 + リフレッシュトークン7日）の実装 |
| 1-03-b | httpOnly Cookie によるトークン格納（localStorage廃止）            |
| 1-03-c | Cookie 属性を `SameSite=Strict; Secure; HttpOnly` に設定   |
| 1-03-d | bcrypt によるパスワードハッシュ化（現行の平文パスワード廃止）                   |
| 1-03-e | ログイン時に JWT ペイロードへユーザ権限（permissions配列）を含める            |
| 1-03-f | アクセストークン自動更新（`/api/auth/refresh` エンドポイント）の実装         |
| 1-03-g | DB管理によるユーザ追加・変更・無効化の実装（ハードコードの廃止）                    |


---

### 1-04 操作ログ・監査証跡の実装 ★★★ ✅ 対応済（主要操作対応）

**対応要件：** 7.17, 12.4, 16.2  
**現状（初版）：** 監査ログなし。誰がいつ何を操作したか追跡不能。

> **対応結果（第3〜5期）：** `audit_logs` テーブルを実装し、承認操作（`approveDocument`/`rejectDocument`）・請求確定（`approveInvoice`）等の主要操作で JSONB 形式の変更前後データ保存を実装。`invoiceService.js` では `withTransaction` でステータス変更と監査ログ INSERT を1トランザクション化（INF-10）。1-04-e（管理画面）・1-04-f（検索API）は未着手。


| #      | 実装内容                                                                                                                                          |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 1-04-a | `audit_logs` テーブルの実装（user_id, user_name, action, entity_type, entity_id, before_data, after_data, ip_address, user_agent, result, created_at） |
| 1-04-b | Fastify hook（preHandler / onSend）による自動ログ記録ミドルウェアの実装                                                                                           |
| 1-04-c | 対象操作の網羅：LOGIN / LOGIN_FAILED / LOGOUT / CREATE / UPDATE / DELETE / SUBMIT_APPROVAL / APPROVE / REJECT / PRINT / EXPORT                        |
| 1-04-d | 変更前後のデータ（`before_data` / `after_data`）を JSONB で保存                                                                                             |
| 1-04-e | 監査ログ参照画面（システム管理者向け）の実装                                                                                                                        |
| 1-04-f | 監査ログ検索API（`GET /api/audit-logs`）の実装（管理者権限必須）                                                                                                  |


---

## 2. セキュリティ強化

### 2-01 サーバー側認可ミドルウェアの実装 ★★★ ✅ 対応済

**対応要件：** 10.1, 10.3, 10.5  
**現状（初版）：** 認証済みなら権限に関わらず全API操作が可能（VA-01）。

> **対応結果（第3〜4期）：** `server/plugins/authorization.js` に `requirePermission(permission)` ミドルウェアを実装。マスタ管理・ユーザ管理・承認依頼・承認実行・支払登録の各 API に権限チェックを追加。Fastify v5 対応として `preHandler` の `throw err` パターン（BUG-02修正、2026-05-06）も適用済み。`authorization.test.js` 全通過確認。フロントエンドの権限参照元も `/api/auth/me` に切り替え済み。


| #      | 実装内容                                                                       |
| ------ | -------------------------------------------------------------------------- |
| 2-01-a | `requirePermission(permission)` ミドルウェアの実装（server/plugins/authorization.js） |
| 2-01-b | マスタ管理APIに `master:edit` 権限チェックを追加                                          |
| 2-01-c | ユーザ管理APIに `user-permission:edit` 権限チェックを追加                                 |
| 2-01-d | 承認依頼APIに `approval:apply`、承認・却下APIに `approval:act` 権限チェックを追加               |
| 2-01-e | 支払実績登録APIに `payment:edit` 権限チェックを追加                                        |
| 2-01-f | フロントエンドの権限参照元を `/api/auth/me` レスポンス（permissions配列）に切り替え（ローカルハードコード廃止）      |


---

### 2-02 JWT秘密鍵管理の強化 ★★★ ✅ 対応済

**対応要件：** 12.3  
**現状（初版）：** `JWT_SECRET` 未設定でも固定のフォールバック鍵で起動できる（VA-02）。

> **対応結果（第4期 SRE-I）：** `NODE_ENV=production` かつ `JWT_SECRET` 未設定時に起動失敗（`process.exit(1)`）する制御を実装。`.env.example` / `.env.production` テンプレートを整備し、必須環境変数（`JWT_SECRET` 等）を明記。


| #      | 実装内容                                                                     |
| ------ | ------------------------------------------------------------------------ |
| 2-02-a | `NODE_ENV=production` かつ `JWT_SECRET` 未設定の場合に起動を失敗させる（`process.exit(1)`） |
| 2-02-b | `.env.example` を整備してデプロイ手順に必須設定を明記                                       |


---

### 2-03 通知の所有者チェック追加 ★★

**現状：** `PUT /api/notifications/:id/read` が他ユーザの通知を既読化できる（VA-03）。


| #      | 実装内容                                              |
| ------ | ------------------------------------------------- |
| 2-03-a | 通知既読化APIに `recipientId === req.user.id` の所有者確認を追加 |
| 2-03-b | 対象が存在しない・所有者が異なる場合に 404 を返す                       |


---

### 2-04 Mass Assignment 対策 ★★

**現状：** Fastify スキーマ未定義で余剰フィールドが内部管理フィールドに混入する可能性（VA-04）。


| #      | 実装内容                                                                                                   |
| ------ | ------------------------------------------------------------------------------------------------------ |
| 2-04-a | ユーザ管理API（登録・更新）の入力スキーマに `additionalProperties: false` を設定し、権限昇格フィールド（passwordHash, permissions等）の混入を防ぐ |
| 2-04-b | 顧客・仕入先・商品マスタAPIに Fastify JSON Schema を追加                                                               |
| 2-04-c | 業務伝票API（見積・受注・発注・請求・支払）にスキーマを順次追加                                                                      |


---

### 2-05 ログイン総当たり防御 ★★ ✅ 対応済

**現状（初版）：** `/api/auth/login` 専用のレートリミットがない（VA-05）。

> **対応結果（第4期 チームC2 P9-B-03）：** `server/routes/auth.js` の `POST /api/auth/login` に `config.rateLimit: { max: 5, timeWindow: '1 minute' }` を設定（本番環境のみ適用。テスト環境での並列実行を阻害しないよう `NODE_ENV === 'production'` 条件付き）。`authService.js` で `user.status !== '有効'` の場合に 401 を返す制御も追加。`auth.test.js`（429/401テスト含む）全通過確認。


| #      | 実装内容                                            |
| ------ | ----------------------------------------------- |
| 2-05-a | ログインルートに専用レートリミット（例：5回/分）を設定                    |
| 2-05-b | 認証サービスでユーザのステータス確認を追加（`status !== '有効'` なら 401） |


---

### 2-06 CSRF 対策の明示化 ★★ ✅ 対応済

**現状（初版）：** SameSite=Strict に依存のみで、明示的な CSRF トークン検証がない（VA-07）。

> **対応結果（第3〜4期）：** `server/plugins/csrf.js` に `@fastify/csrf-protection` を使った CSRF トークン検証を実装。全状態変更 API（POST/PATCH/DELETE）に適用。Fastify v5 対応として `preHandler` の `throw err` パターン（BUG-03修正、2026-05-06）も適用済み。`csrf.test.js` 8件全通過確認。フロントエンドの `apiFetch` に CSRF トークン取得・送信ロジックを組み込み済み。


| #      | 実装内容                                                                        |
| ------ | --------------------------------------------------------------------------- |
| 2-06-a | `@fastify/csrf-protection` を導入して全状態変更API（POST/PATCH/DELETE）に CSRF トークン検証を追加 |
| 2-06-b | フロントエンドの `apiFetch` に CSRF トークンの取得・送信ロジックを組み込む                              |


---

### 2-07 JWT 失効・セッション管理 ★ ✅ 対応済（主要機能）

**現状（初版）：** 発行済み JWT をサーバー側で失効できない（VA-06）。

> **対応結果（第4期 チームC2 T-11）：** `server/services/refreshTokenService.js` に `verifyAndRotate()` を実装。旧トークン失効＋新トークン発行＋盗用検知（失効済みトークン再利用→全セッション無効化）が動作。`server/repositories/refreshTokenRepository.js`（save/findById/revoke/revokeAllForUser）も実装済み。`refreshTokenService.test.js` 10件全通過。2-07-d（ローテーション）・2-07-e（ユーザ停止時の一括無効化）も本機能の実装で対応済み。


| #      | 実装内容                                             |
| ------ | ------------------------------------------------ |
| 2-07-a | JWT に `jti`（UUID）を追加                             |
| 2-07-b | セッションリポジトリ（インメモリ or DB）を実装してログイン・ログアウト時にセッションを管理 |
| 2-07-c | JWT 検証時にセッション有効性を確認                              |
| 2-07-d | （将来対応）リフレッシュトークンのローテーションと再利用検知                   |
| 2-07-e | （将来対応）ユーザ停止時の既存セッション一括無効化                        |


---

### 2-08 XSS 対策・CSP 設定 ★ ✅ 対応済

**現状（初版）：** `app.js` の `innerHTML` ベースの大面積HTML生成で escape 漏れリスクがある（VA-09）。

> **対応結果（第4期 チームC2 P9-E-BE）：** `server/app.js` に `@fastify/helmet` を導入し、`contentSecurityPolicy.directives` を明示設定（`default-src: 'self'`, `script-src: 'self'`, `style-src: 'self' 'unsafe-inline'`, `object-src: 'none'`, `frame-ancestors: 'none'` 等）。`security.test.js` （CSPヘッダー検証5件）全通過確認。`app.js` の動的値への `escapeHtml` 適用は部分対応。


| #      | 実装内容                                                                           |
| ------ | ------------------------------------------------------------------------------ |
| 2-08-a | `app.js` 内の全動的値について `escapeHtml` 適用漏れを修正（VA-09 指摘箇所を含む）                        |
| 2-08-b | `@fastify/helmet` に CSP を明示設定（`defaultSrc: ['self']`, `scriptSrc: ['self']` 等） |


---

## 3. フロントエンド未実装機能

### 3-01 S-07 納品・検収の独立画面化 ★★

**対応要件：** 8.2.7  
**現状：** S-06（発注詳細）に内包されており、独立したナビゲーション項目が存在しない。


| #      | 実装内容                               |
| ------ | ---------------------------------- |
| 3-01-a | S-07 を独立したナビゲーション項目として追加           |
| 3-01-b | 納品実績一覧（発注コード・ステータス・納品日での絞り込み付き）を実装 |
| 3-01-c | 一覧行から発注詳細（S-06）への遷移リンクを実装          |


---

### 3-02 S-14 通知ロジックの完全実装 ★★

**対応要件：** 7.11  
**現状：** 通知一覧画面の枠のみ。通知の生成・配信ロジックが未実装。


| #      | 実装内容                                           | 通知ID |
| ------ | ---------------------------------------------- | ---- |
| 3-02-a | 承認依頼送信時に次ステップ承認者へ通知を生成                         | N-01 |
| 3-02-b | 承認完了時に申請者へ通知を生成                                | N-02 |
| 3-02-c | 却下時に申請者へ却下コメント付き通知を生成                          | N-03 |
| 3-02-d | 承認依頼から規定日数（初期値3日）経過後に申請者へ滞留通知を生成（バッチ or ポーリング） | N-04 |
| 3-02-e | 請求の支払期日当日に伝票作成者へ通知を生成                          | N-05 |
| 3-02-f | 発注の納品予定日当日に伝票作成者へ通知を生成                         | N-06 |
| 3-02-g | 通知一覧で未読/既読・種別・発生日の絞り込みを有効化                     |      |
| 3-02-h | 通知クリックで対象伝票・画面へ遷移                              |      |


---

### 3-03 S-15 承認条件設定UIの完全実装 ★★

**対応要件：** 7.11, 8.2.11, 15.1  
**現状：** 会社情報・決算月の設定のみ。承認ルート・社長決裁条件の設定UIが未実装。


| #      | 実装内容                                     |
| ------ | ---------------------------------------- |
| 3-03-a | 承認ルート設定UI（伝票種別・ステップ番号・承認者ユーザIDの登録・更新・削除） |
| 3-03-b | 社長決裁条件の閾値（見積金額上限・利益率下限）を管理画面から変更できる機能    |
| 3-03-c | 承認滞留判定日数（N-04）を管理画面から変更できる機能             |


---

### 3-04 見積の複製・改版・版管理の完全実装 ★★

**対応要件：** 7.3, 8.2.4  
**現状：** `src/quotation.js` に `createRevision` 関数は実装済みだが、旧版の保持・版数表示・最新版識別のUIが不完全。


| #      | 実装内容                            |
| ------ | ------------------------------- |
| 3-04-a | 見積の複製機能（既存見積を元にした新しい下書き作成）のUI実装 |
| 3-04-b | 改版時に旧版を履歴として保持し、最新版を一覧上で識別できる表示 |
| 3-04-c | 見積詳細で版数の履歴を参照できるUI              |


---

### 3-05 添付ファイル管理の完全実装 ★★

**対応要件：** 9.4  
**現状：** フロントエンドのみ。ファイルが永続化されない。権限制御なし。


| #      | 実装内容                                            |
| ------ | ----------------------------------------------- |
| 3-05-a | ファイルアップロードAPIの実装（@fastify/multipart）とストレージへの永続化 |
| 3-05-b | 権限のない利用者が添付ファイルを閲覧できないアクセス制御の実装                 |
| 3-05-c | 添付ファイルの削除・更新履歴の保持                               |
| 3-05-d | 受注の添付ファイル（契約書・注文書）をAPIで保存・取得する実装                |


---

### 3-06 承認状況エリアの完全実装 ★★

**対応要件：** 8.2.4, 8.2.5, 8.2.6  
**現状：** 承認履歴テーブル（操作日時・操作者・操作種別・コメント）のUI枠はあるが、実データが連携されていない。


| #      | 実装内容                                                   |
| ------ | ------------------------------------------------------ |
| 3-06-a | 見積・受注・発注の詳細画面下部に現在の承認ステップ（例：「ステップ 1/2：営業部長 確認待ち」）を動的表示 |
| 3-06-b | 承認履歴テーブル（操作日時・操作者・操作種別・コメント）をDBから取得して表示                |
| 3-06-c | 承認履歴は申請者・承認者の両方から閲覧可能にする                               |


---

### 3-07 複数ステップ承認フローの実装 ★★

**対応要件：** 7.11  
**現状：** 単ステップの承認・却下は実装済みだが、複数ステップ（例：営業部長→社長）のフロー制御が未実装。


| #      | 実装内容                                              |
| ------ | ------------------------------------------------- |
| 3-07-a | 承認ルートの複数ステップ管理（ステップ番号・承認者IDの管理）                   |
| 3-07-b | AND条件（同一ステップに複数承認者が設定された場合、全員承認でステップ完了）の実装        |
| 3-07-c | ステップ完了後に次ステップへ自動進行する制御                            |
| 3-07-d | 社長決裁条件（`requiresPresidentApproval`）に基づく動的ルート分岐の実装 |
| 3-07-e | 途中ステップで却下された場合に申請者の下書き状態に戻す制御                     |


---

### 3-08 ダッシュボードの権限別表示制御 ★

**対応要件：** 8.2.2  
**現状：** 承認待ち件数など指標は表示されているが、ログインユーザが承認者として割り当てられている件数のみを集計する制御が不完全。


| #      | 実装内容                                          |
| ------ | --------------------------------------------- |
| 3-08-a | 承認待ち件数を「自分が承認者として割り当てられている件数のみ」に絞り込む（自己申請は除外） |
| 3-08-b | 件数ゼロの伝票種別をグレーアウト表示する                          |
| 3-08-c | 各指標クリックで対象一覧画面へ遷移（種別フィルター付き）する                |


---

### 3-09 ダッシュボードのユーザカスタマイズ ☆ 将来対応

**対応要件：** 要件定義外（第2回チームレビューで追加）  
**現状：** ダッシュボードの表示内容・レイアウトはシステム固定。ユーザ個別の設定保存機能なし。  
**前提条件：** 1-01（バックエンドAPI）・1-02（DB永続化）・1-03（認証基盤）の完了後に着手可能。


| #      | 実装内容                                                                  |
| ------ | --------------------------------------------------------------------- |
| 3-09-a | `user_preferences` テーブルの追加（ユーザID・設定キー・設定値JSONBの構造）                    |
| 3-09-b | 表示するウィジェット（承認待ち・未収・未払・売上見込・担当案件一覧等）をユーザが選択して追加/削除できるUI                |
| 3-09-c | ウィジェットの表示順をドラッグ&ドロップで変更できるUI                                          |
| 3-09-d | カスタマイズ設定をAPIで保存・取得する実装（`GET/PUT /api/users/me/preferences/dashboard`） |
| 3-09-e | 管理者がデフォルトレイアウトを設定できる機能（個別設定のないユーザに適用）                                 |


---

## 4. 業務ロジックの未実装・不完全項目

### 4-01 帳票PDF出力の完全実装 ★★

**対応要件：** 7.14, 8.3  
**現状：** 見積書・発注書・請求書はHTMLプレビューのみ。その他帳票は未実装。


| #      | 実装内容                                       | 対象帳票    |
| ------ | ------------------------------------------ | ------- |
| 4-01-a | ブラウザ印刷 / PDF ライブラリ（例：Puppeteer）による確実なPDF出力 | 全帳票     |
| 4-01-b | 受注確認書のPDF出力                                | 受注確認書   |
| 4-01-c | 納品書のPDF出力                                  | 納品書     |
| 4-01-d | 支払通知書のPDF出力                                | 支払通知書   |
| 4-01-e | 売上集計表のPDF出力                                | 売上集計表   |
| 4-01-f | 粗利集計表のPDF出力                                | 粗利集計表   |
| 4-01-g | 未収一覧のPDF出力                                 | 未収一覧    |
| 4-01-h | 未払一覧のPDF出力                                 | 未払一覧    |
| 4-01-i | 請求書の一覧からの一括出力                              | 請求書（複数） |


---

### 4-02 CSV出力・CSV一覧ダウンロードの実装 ★★

**対応要件：** 7.12, 8.2  
**現状：** 一部画面にCSV出力ボタンは存在するが、実際に全画面でダウンロード可能になっているか不確か。永続化なし段階で実データが存在しない。


| #      | 実装内容                                              |
| ------ | ------------------------------------------------- |
| 4-02-a | 全一覧画面（案件・見積・受注・発注・納品・請求・入金・支払）のCSV出力を永続化後のデータで有効化 |
| 4-02-b | レポート画面（売上集計・未収・未払）のCSV出力の実装                       |
| 4-02-c | CSV出力操作を監査ログに記録（EXPORT）                           |


---

### 4-03 月次締め処理 ★★★（チームレビューにより★★から引き上げ）

**対応要件：** 6.6, 7.7  
**現状：** 未実装。


| #      | 実装内容                         |
| ------ | ---------------------------- |
| 4-03-a | 顧客ごとの締日設定（マスタ）に基づく請求対象の自動抽出  |
| 4-03-b | 支払予定日単位での支払対象の抽出             |
| 4-03-c | 月次売上・月次原価・粗利を案件別・取引先別に集計する処理 |


---

### 4-04 失注・取消の記録機能 ★★（チームレビューにより★から引き上げ）

**対応要件：** 7.3, 6.5  
**現状：** `src/quotation.js` にステータス定義はあるが、失注理由を記録するUI・保存処理が未実装。


| #      | 実装内容                     |
| ------ | ------------------------ |
| 4-04-a | 見積の失注登録UI（理由入力フォーム付き）の実装 |
| 4-04-b | 失注理由・取消理由のDB保存           |
| 4-04-c | 失注・取消一覧の参照機能             |


---

### 4-05 請求確定後の訂正・取消処理 ★★（チームレビューにより★から引き上げ）

**対応要件：** 6.5, 7.7  
**現状：** ステータス上は「取消」が存在するが、訂正伝票の発行フローが未実装。


| #      | 実装内容                          |
| ------ | ----------------------------- |
| 4-05-a | 確定後の請求書に対する取消操作と取消理由の記録       |
| 4-05-b | 訂正伝票の作成（元の請求を参照した新規請求を作成する機能） |


---

### 4-06 一部発注・数量変更時の変更履歴管理 ★

**対応要件：** 6.5  
**現状：** 変更履歴の保持機能が未実装。


| #      | 実装内容                  |
| ------ | --------------------- |
| 4-06-a | 発注後の数量変更操作に変更前後の履歴を記録 |
| 4-06-b | 発注詳細画面で変更履歴を参照できるUI   |


---

### 4-07 ユーザ個別権限の追加・制限設定 ★

**対応要件：** 10.3  
**現状：** ユーザマスタの基本情報は実装済みだが、機能単位・操作単位での個別権限の付与・制限UIが不完全。


| #      | 実装内容                                    |
| ------ | --------------------------------------- |
| 4-07-a | ユーザマスタ編集画面で個別権限（参照・作成・更新・削除・承認・出力）の設定UI |
| 4-07-b | 標準権限（部門・役職ベース）に対する追加付与または制限の設定          |
| 4-07-c | 個別権限設定の変更履歴を保持                          |


---

## 5. テスト強化

### 5-01 却下→修正→再申請の E2E テスト ★★★

**現状：** 承認・却下の個別操作は確認されているが、一連フローが未テスト。


| #      | 実装内容                           |
| ------ | ------------------------------ |
| 5-01-a | 見積が却下→申請者が明細修正→再申請→最終承認の E2E   |
| 5-01-b | 発注が却下→申請者が単価修正→再申請→最終承認の E2E   |
| 5-01-c | 請求が却下→申請者が金額修正→再申請→承認の E2E     |
| 5-01-d | 支払依頼が却下→申請者が支払金額修正→再申請→承認の E2E |


---

### 5-02 各画面バリデーション E2E テストの分散 ★★★ ✅ 対応済

**現状（初版）：** バリデーションテストが1ファイルに集約されており、各画面のspecから分離されている。

> **対応結果（第6期 QA-F P10-RT-02）：** 各業務画面 spec.js にバリデーション describe ブロックを追加（見積・受注・発注・請求・支払・入金・顧客マスタ・ユーザマスタの8ファイル）。19件全通過（workers=4 並列実行 31秒）。BUG-E（user タブ入力が customerForm に同期されていた `app.js` 不具合）も本作業で修正。


| #      | 実装内容                                                                     |
| ------ | ------------------------------------------------------------------------ |
| 5-02-a | 各業務画面 spec.js に「バリデーション」describe ブロックを追加（見積・受注・発注・請求・支払・入金・顧客マスタ・ユーザマスタ） |


---

### 5-03 複数ステップ承認の途中却下 E2E ★★ ✅ 対応済

**現状（初版）：** 単ステップの承認は確認済みだが、途中ステップ却下時の挙動が未テスト。

> **対応結果（第4期 QA-H P10-RT-03）：** `e2e/approval-multistep.spec.js` を新規作成。承認ルート設定確認（UI）＋途中却下フローをカバー（8テスト）。8件全通過確認。5-03-a（2ステップ目却下→下書き復帰）・5-03-b（承認履歴記録）の両シナリオをカバー済み。


| #      | 実装内容                                    |
| ------ | --------------------------------------- |
| 5-03-a | 2ステップ承認で第2ステップが却下→申請者に「却下」通知→下書きに戻る E2E |
| 5-03-b | 承認履歴に「ステップ1: 承認」「ステップ2: 却下」が記録される E2E   |


---

### 5-04 発注→納品→請求のデータ連鎖整合性 E2E ★★ ✅ 対応済

**現状（初版）：** 個別画面のE2Eはあるが、一連のデータ連鎖を検証するテストがない。

> **対応結果（第6期 QA-F+QA-I P10-RT-04）：** `e2e/zzz-data-chain.spec.js` に発注→納品（既存7件）＋入金消込（新規2件）の計9件を実装。一部納品後の発注残数量変化・全数納品後の「納品済」ステータス自動遷移・納品後の請求金額整合性・全額入金後の消込済みステータス・一部入金時の未収残高表示をカバー。9件全通過確認。


| #      | 実装内容                     |
| ------ | ------------------------ |
| 5-04-a | 一部納品後の発注残数量変化の確認         |
| 5-04-b | 全数納品後の「納品済」ステータス自動遷移の確認  |
| 5-04-c | 納品後の請求起票で請求金額と発注金額の整合性確認 |


---

### 5-05 サーバー側認可テスト（APIレベル） ★★★ ✅ 対応済（5-05-e/f）

**現状（初版）：** 画面ボタン非表示のみ。API直接呼び出しで権限チェックを確認するテストがない。

> **対応結果（第4期 QA-H AT-01）：** `server/routes/auth.test.js` に以下を確認・通過。5-05-e（連続ログイン失敗→429）・5-05-f（停止ユーザログイン→401）の2件を34件テストスイートで確認済み。5-05-a〜d（`master:edit`/`approval:act`/`user-permission:edit` なしユーザの403・他ユーザ通知の404）は未着手。


| #      | 実装内容                                                    |
| ------ | ------------------------------------------------------- |
| 5-05-a | `master:edit` なしユーザでマスタ更新API呼び出し → 403 のAPIテスト          |
| 5-05-b | `approval:act` なしユーザで承認・却下API呼び出し → 403 のAPIテスト         |
| 5-05-c | `user-permission:edit` なしユーザでユーザ管理API呼び出し → 403 のAPIテスト |
| 5-05-d | 他ユーザの通知IDで既読化API呼び出し → 404 のAPIテスト                      |
| 5-05-e | ログイン連続失敗 → 429 のAPIテスト                                  |
| 5-05-f | 停止ユーザのログイン → 401 のAPIテスト                                |


---

### 5-06 大量データ・ページネーション E2E ★

**現状：** ページサイズ変更は確認済みだが、実データ件数でのページネーション動作が未確認。


| #      | 実装内容                            |
| ------ | ------------------------------- |
| 5-06-a | 21件以上の見積が存在するとき一覧が複数ページに分割されること |
| 5-06-b | 「次のページ」遷移で2ページ目のデータが表示されること     |
| 5-06-c | キーワード検索後のヒット件数が一覧ヘッダーに表示されること   |


---

### 5-07 同時操作・競合シナリオ ☆ 将来対応

**現状：** DB永続化が完了した段階で対応。


| #      | 実装内容                            |
| ------ | ------------------------------- |
| 5-07-a | 複数ユーザが同一伝票を同時編集した場合の楽観的ロック・警告表示 |
| 5-07-b | 2ユーザが同一伝票を同時承認した場合の二重承認防止       |


---

## 6. 外部連携（将来対応）

**対応要件：** 11.1, 11.2


| #    | 連携先      | 実装内容                                  | 優先度 |
| ---- | -------- | ------------------------------------- | --- |
| 6-01 | 会計システム   | 売上・請求・入金・支払データのCSVエクスポート              | ☆   |
| 6-02 | 電子契約システム | 受注時の契約書を電子契約システムと連携                   | ☆   |
| 6-03 | メール送信基盤  | N-01〜N-06 の通知をメールでも送信（初期リリースは画面内通知のみ） | ☆   |
| 6-04 | SSO認証基盤  | 外部認証（SAML/OIDC）によるシングルサインオン           | ☆   |
| 6-05 | 銀行データ連携  | 入金データの自動取込・消込補助                       | ☆   |
| 6-06 | 将来のAPI連携 | CSV連携からAPIベース（REST/Webhook）への拡張       | ☆   |


---

## 7. 初期データ移行

**対応要件：** 14.1, 14.2


| #    | 実装内容                       | 優先度 |
| ---- | -------------------------- | --- |
| 7-01 | 顧客マスタのCSVインポート機能           | ★   |
| 7-02 | 仕入先マスタのCSVインポート機能          | ★   |
| 7-03 | 商品・サービスマスタのCSVインポート機能      | ★   |
| 7-04 | ユーザマスタのCSVインポート機能          | ★   |
| 7-05 | 案件残・受注残・請求残・未払残の移行用インポート機能 | ★   |
| 7-06 | 移行データのクレンジング・整合性確認手順の整備    | ★   |


---

---

## 8. 業務ロジック追加・マスタ管理補完（チームレビューで追加）

### 8-01 発注の仕入先別分割起票機能 ★★

**対応要件：** 6.3.2, 7.5, 8.2.6  
**現状：** バックログ1-01-bに暗黙的に含まれているだけで独立した実装項目として管理されていない。


| #      | 実装内容                                 |
| ------ | ------------------------------------ |
| 8-01-a | 受注明細から発注起票時に仕入先を指定して明細を割り当てる操作UI     |
| 8-01-b | 複数仕入先が存在する場合に「仕入先別に分割して発注を作成する」操作フロー |
| 8-01-c | 分割された各発注が個別に承認依頼・発注書出力できることの確認       |


---

### 8-02 複数受注の合算請求と消費税一括計算 ★★★

**対応要件：** 7.7  
**現状：** 未実装。消費税計算の誤りは法的リスクに直結する。


| #      | 実装内容                                              |
| ------ | ------------------------------------------------- |
| 8-02-a | 請求作成時に複数受注を選択して1枚の請求書に合算する操作UI                    |
| 8-02-b | 合算請求における消費税を「課税合計に対して一括計算」するロジック（明細ごとの税額合計方式ではない） |
| 8-02-c | 分割請求（1受注を複数回に分けて請求する）の実装                          |


---

### 8-03 顧客マスタへの締日・支払サイト・請求先情報管理 ★★★

**対応要件：** 7.7, 7.10  
**現状：** 月次締め処理（4-03）の前提条件であるが、マスタ管理の実装項目として独立していない。


| #      | 実装内容                           |
| ------ | ------------------------------ |
| 8-03-a | 顧客マスタに締日（末日・15日等）を登録・編集できるUI   |
| 8-03-b | 顧客マスタに支払サイト（翌月末等）を登録・編集できるUI   |
| 8-03-c | 顧客マスタに請求先（顧客本社・別法人等）を登録できるUI   |
| 8-03-d | 請求対象抽出時に締日・支払サイトを参照して自動フィルタリング |


---

### 8-04 受注承認依頼の業務バリデーション（サーバー側） ★★★

**対応要件：** 6.1（4.1〜4.3）  
**現状：** フロント側のチェックのみ。サーバー側APIに業務ルールの強制がない。


| #      | 実装内容                                                               |
| ------ | ------------------------------------------------------------------ |
| 8-04-a | 受注承認依頼API（`POST /api/orders/:code/submit-approval`）に見積紐付け必須チェックを追加 |
| 8-04-b | 受注合計金額と紐付き見積合計金額の一致チェック（不一致なら400エラー）                               |
| 8-04-c | 承認依頼送信時に添付ファイル1件以上の存在チェック（下書き保存時は不要）                               |
| 8-04-d | 上記バリデーション失敗時のフロント側エラーメッセージ表示                                       |


---

### 8-05 顧客担当者マスタの管理 ★★

**対応要件：** 9.1, 9.2  
**現状：** ERに `CUSTOMER_CONTACT` エンティティが定義されているが実装項目に存在しない。


| #      | 実装内容                                  |
| ------ | ------------------------------------- |
| 8-05-a | 顧客担当者（氏名・部署・メールアドレス・電話番号）の登録・編集・無効化UI |
| 8-05-b | 顧客詳細画面に担当者一覧タブを追加                     |
| 8-05-c | 見積書・請求書の帳票宛先に顧客担当者名・部署を印字             |


---

### 8-06 部門マスタ・役職マスタの管理画面 ★★

**対応要件：** 5.1, 7.10, 8.2.11  
**現状：** ユーザの部門・役職設定の前提となるマスタが管理画面に未実装。  
**補足（第2回チームレビュー）：** 本項目は「部門の追加・名称変更・削除」要望に対応する。システム全体の方針に従い削除は論理削除（無効化）で対応する。部門に紐付くユーザが存在する場合の無効化制御を8-06-dとして追加。


| #      | 実装内容                                                           |
| ------ | -------------------------------------------------------------- |
| 8-06-a | 部門マスタ（部門名・部門コード・並び順）の一覧・登録・編集・無効化UI                            |
| 8-06-b | 役職マスタ（役職名・承認権限レベル）の一覧・登録・編集UI                                  |
| 8-06-c | ユーザマスタの部門・役職選択をマスタ参照プルダウンに変更                                   |
| 8-06-d | 所属ユーザが存在する部門を無効化しようとした場合の警告表示と確認ダイアログ（無効化後は該当ユーザの部門欄を「未設定」に変更） |


---

### 8-07 支払先口座情報（仕入先）の管理 ★★

**対応要件：** 8.2.10  
**現状：** 支払通知書への口座情報印字のために必要だが独立した実装項目として存在しない。


| #      | 実装内容                                      |
| ------ | ----------------------------------------- |
| 8-07-a | 仕入先マスタに支払先口座情報（金融機関名・支店名・口座種別・口座番号）を登録・編集 |
| 8-07-b | 支払依頼作成時に仕入先マスタから口座情報を自動セット                |
| 8-07-c | 支払通知書PDFへの支払先口座情報の印字                      |


---

### 8-08 過入金時の調整伝票・手数料差引の消込処理 ★★

**対応要件：** 7.8, 6.5  
**現状：** `src/receipt.js` に `calcExcessAmount` は実装済みだが、調整伝票の作成フロー・UIが未実装。


| #      | 実装内容                                 |
| ------ | ------------------------------------ |
| 8-08-a | 過入金発生時の調整伝票（差額処理）作成UIと保存ロジック         |
| 8-08-b | 振込手数料差引入金の消込処理（手数料を差し引いた実入金額で消込する機能） |
| 8-08-c | 消込画面での不足額・過入金額の視覚的表示                 |


---

### 8-09 受注の「契約手続き済」ステータス管理 ★★

**対応要件：** 6.2.2  
**現状：** `src/order.js` に `completeContractProcedure` 関数は実装済みだが、管理部長限定の操作UIが存在しない。


| #      | 実装内容                                 |
| ------ | ------------------------------------ |
| 8-09-a | 受注詳細画面に「契約手続き済にする」ボタンを実装（管理部長権限のみ表示） |
| 8-09-b | 操作時に「承認済み」→「契約手続き済」のステータス変更APIを呼び出す  |
| 8-09-c | 契約手続き済ステータスの受注一覧での視覚的識別              |


---

### 8-10 アカウントロックとパスワード変更要求 ★★ ✅ 対応済（8-10-a/b）

**対応要件：** 8.2.1  
**現状（初版）：** バックログ2-05はレートリミット（一時的な防御）のみ。アカウントロック（状態の永続化）が別の概念として未定義。

> **対応結果（第4期 チームC2 T-10）：** `server/services/authService.js` に `LOCK_THRESHOLD=5`・`LOCK_DURATION_MS=30分` のアカウントロック判定・カウント更新を実装。`server/repositories/userRepository.js` に `updateLoginState(id, {failedLoginCount, lockedUntil})` を追加。`server/db/migrations/002_account_lock.sql` で `users` テーブルに `failed_login_count`・`locked_until` カラムを追加。`authService.test.js`（ロック関連5テスト）全通過確認。8-10-c（初回ログイン時パスワード変更要求）は未着手。


| #      | 実装内容                                  |
| ------ | ------------------------------------- |
| 8-10-a | N回連続ログイン失敗でアカウントを「ロック」状態にする（初期値：5回）   |
| 8-10-b | ロック状態の解除：管理者によるロック解除操作UI              |
| 8-10-c | 初回ログイン時またはシステム管理者が設定した場合のパスワード変更要求フロー |
| 8-10-d | ログイン画面でのロック状態・パスワード変更要求の通知表示          |


---

### 8-11 案件詳細画面の財務集計表示 ★★

**対応要件：** 7.2, 8.2.3  
**現状：** S-13レポート画面とは別に、案件詳細（S-03）でインライン確認が必要だが未実装。


| #      | 実装内容                              |
| ------ | --------------------------------- |
| 8-11-a | 案件詳細画面に売上・原価・粗利・未収・未払の集計サマリエリアを追加 |
| 8-11-b | 紐付く受注・発注・請求・入金・支払を横断して集計するAPI     |
| 8-11-c | 案件詳細から各関連伝票一覧への遷移リンク              |


---

### 8-12 S-13レポート画面の絞り込み条件UI ★★

**対応要件：** 8.2.13  
**現状：** バックログ1-01-eにレポートAPIはあるが、フロントエンドの集計条件UIが独立した実装項目として存在しない。


| #      | 実装内容                               |
| ------ | ---------------------------------- |
| 8-12-a | 売上・原価・粗利集計の絞り込み条件UI（期間・部門・担当者・取引先） |
| 8-12-b | 未収一覧・未払一覧・承認待ち一覧のレポート画面への追加        |
| 8-12-c | 承認待ち一覧のPDF/CSV出力（帳票バックログ4-01に未記載）  |


---

### 8-13 マスタ種別ごとの変更履歴確認機能 ★

**対応要件：** 8.2.11  
**現状：** バックログ1-04（監査ログ）で操作ログは記録されるが、マスタ別の変更履歴参照UIが独立した項目として存在しない。


| #      | 実装内容                               |
| ------ | ---------------------------------- |
| 8-13-a | 顧客・仕入先・商品・ユーザ等のマスタ詳細画面に「変更履歴」タブを追加 |
| 8-13-b | 変更前後の値・変更者・変更日時を一覧表示する             |


---

### 8-14 レポート画面のグラフ表示機能 ★

**対応要件：** 要件定義外（第2回チームレビューで追加）  
**現状：** S-13レポート画面は集計表（数値テーブル）のみ。グラフ表示機能が存在しない。8-12（絞り込み条件UI）と連動して実装する。  
**前提条件：** 1-01-e（レポートAPI）・8-12（絞り込み条件UI）の完了後に着手可能。


| #      | 実装内容                                            |
| ------ | ----------------------------------------------- |
| 8-14-a | Chart.js を導入し、売上・費用（原価）・利益（粗利）を棒グラフまたは折れ線グラフで表示 |
| 8-14-b | 表示単位をユーザが切り替えられるセレクタUIの実装（全体 / 顧客ごと / 案件ごと）     |
| 8-14-c | 8-12の絞り込み条件（期間・部門・担当者・取引先）と連動してグラフをリアルタイム更新     |
| 8-14-d | グラフ種別の切り替え（棒グラフ：比較表示 / 折れ線グラフ：月次推移）のUI          |
| 8-14-e | グラフの画像としてのエクスポート機能（PNG出力）                       |
| 8-14-f | グラフ集計値の単体テスト（APIレスポンスデータからグラフ用データへの変換ロジック）      |


---

## 9. インフラ・運用基盤（チームレビューで追加）

### 9-01 DBバックアップ・障害復旧設計 ★★★ ✅ 対応済

**対応要件：** 12.2  
**現状（初版）：** バックログ全体にバックアップ・リストアに関する記載がまったくない。

> **対応結果（第4期 SRE-I INF-02）：** `scripts/backup.sh`（pg_dump + gzip + 7日保持 + S3オプション対応）を作成。`docs/restore-procedure.md` に RTO 4時間 / RPO 1時間目標と全リストア手順を記載。WAL アーカイブによる継続的バックアップは本スクリプトで対応。


| #      | 実装内容                               |
| ------ | ---------------------------------- |
| 9-01-a | PostgreSQL WALアーカイブによる継続的バックアップの設定 |
| 9-01-b | 定期スナップショット（日次フルバックアップ）の自動化         |
| 9-01-c | リストア手順書の作成・リストア動作確認                |
| 9-01-d | RTO/RPO目標値の策定（例：RTO 4時間・RPO 1時間）   |


---

### 9-02 HTTPS/TLS設定と証明書管理 ★★★ ✅ 対応済

**対応要件：** 12.3  
**現状（初版）：** 認証・認可の実装は詳細に計画されているが、通信暗号化のインフラ設定が未記載。

> **対応結果（第4期 SRE-I INF-03）：** `infra/nginx.conf` に HTTP→HTTPS リダイレクト・TLS1.2/1.3・HSTS ヘッダー・app:3000 へのリバースプロキシを設定。`scripts/init-tls.sh` で Let's Encrypt certbot による初回証明書取得を自動化。`.env.example` に HTTPS 必須設定を明記。


| #      | 実装内容                                      |
| ------ | ----------------------------------------- |
| 9-02-a | 本番環境でのTLS証明書取得・設定（Let's Encrypt または有料証明書） |
| 9-02-b | リバースプロキシ（nginx / Caddy 等）でのTLS終端設定        |
| 9-02-c | HTTP→HTTPSリダイレクトの強制                       |
| 9-02-d | `.env.example` へのHTTPS必須設定の明記             |


---

### 9-03 アプリケーションログ収集・集約基盤 ★★★ ✅ 対応済（9-03-a）

**対応要件：** 12.4, 13.2  
**現状（初版）：** 1-04は業務監査ログのみ。アプリケーションエラー・HTTPアクセス・スロークエリの収集基盤がない。

> **対応結果（第4期 SRE-I）：** `pino-pretty` を devDependencies に追加し Pino structured logging を設定（9-03-a 対応）。9-03-b（エラー/アクセス/スロークエリ収集）・9-03-c（ログ集約先転送）・9-03-d（ローテーション）は未着手。


| #      | 実装内容                                             |
| ------ | ------------------------------------------------ |
| 9-03-a | Fastifyへのstructured logging（Pino）設定              |
| 9-03-b | エラーログ・アクセスログ・スロークエリログの収集                         |
| 9-03-c | ログ集約先への転送設定（CloudWatch Logs / Loki 等、インフラに応じて選定） |
| 9-03-d | ログのローテーション・保存期間設定                                |


---

### 9-04 ヘルスチェック・死活監視・アラート ★★★

**対応要件：** 12.2  
**現状：** 稼働監視に関する項目が一切ない。障害を検知できない状態では SLO を担保できない。


| #      | 実装内容                                    |
| ------ | --------------------------------------- |
| 9-04-a | `GET /api/health` エンドポイントの実装（DB接続確認を含む） |
| 9-04-b | 外形監視（Uptime監視）の設定                       |
| 9-04-c | エラーレート上昇・応答時間悪化時のアラート通知設定               |


---

### 9-05 CI/CDパイプライン構築 ★★★ ✅ 対応済

**対応要件：** 13.2（保守容易性）、CLAUDE.md（TDD継続運用）  
**現状（初版）：** バックログにデプロイパイプラインの記載がまったくない。

> **対応結果（第4期 SRE-I INF-01）：** `.github/workflows/ci.yml` に3ジョブ構成を実装。unit-test（vitest + 行カバレッジ80%閾値チェック）・e2e-test（Playwright Chromium + PostgreSQL service container）・deploy-staging（main ブランチのみ・ヘルスチェック確認付き）。`scripts/deploy-staging.sh`（SSH リモートデプロイ）も作成。`vitest.config.js` に coverage 設定を追加。


| #      | 実装内容                                        |
| ------ | ------------------------------------------- |
| 9-05-a | GitHub Actions 等でのユニットテスト自動実行（PRトリガー）       |
| 9-05-b | E2Eテスト自動実行（マージ後トリガー）                        |
| 9-05-c | ステージング環境への自動デプロイ                            |
| 9-05-d | 本番環境への手動承認デプロイフローの整備                        |
| 9-05-e | テストカバレッジレポートのCI上での自動計測・閾値チェック（CLAUDE.md 要件） |


---

### 9-06 環境分離（開発・ステージング・本番） ★★★ ✅ 対応済

**対応要件：** 12.2, 12.3  
**現状（初版）：** ローカル（Docker Compose）と本番の2環境のみ言及。ステージング未定義。

> **対応結果（第4期 SRE-I INF-06）：** `.env.development`（開発用ダミー値・リポジトリ管理）・`.env.staging`（gitignored テンプレート）・`.env.production`（gitignored テンプレート）・`.env.example`（CORS_ORIGIN/RATE_LIMIT_MAX/LOG_LEVEL/DOMAIN/TLS_EMAIL 等を明記）を作成。`docker-compose.staging.yml`（DB + app + nginx + certbot 構成）・`docs/infra-envs.md`（環境設計・構築手順）も整備。


| #      | 実装内容                                       |
| ------ | ------------------------------------------ |
| 9-06-a | ステージング環境の定義と構築                             |
| 9-06-b | 環境別 `.env` 管理方針の策定                         |
| 9-06-c | インフラ定義コード（Terraform / Docker Compose 等）の整備 |


---

### 9-07 DBインデックス設計・クエリ最適化 ★★★ ✅ 対応済

**対応要件：** 12.1（一覧画面3秒以内・CSV1万件）  
**現状（初版）：** 1-02はスキーマ定義のみ。性能目標達成のためのインデックスが設計されていない。

> **対応結果（第5期 チームB+SRE-I INF-07、2026-05-06）：** `server/db/migrations/004_indexes.sql` を作成し8インデックスを追加（複合：quotations/status+issue_date・invoices/status+due_date・invoices/customer_id+status・payments/supplier_id+status、単一：orders/purchase_orders/payments の status、Partial：quotations/status='承認依頼中'）。`scripts/seed-staging.js` で各テーブル1,000件以上のステージングデータを生成し `EXPLAIN ANALYZE` で全代表クエリ1ms未満（3秒要件に対して最大128倍改善）を確認。`004_indexes.test.js` 10件全通過。M7-4 達成。


| #      | 実装内容                                  |
| ------ | ------------------------------------- |
| 9-07-a | 一覧検索（ステータス・顧客・日付範囲）に対する複合インデックスの設計    |
| 9-07-b | 外部キー列・承認待ち集計・未収未払集計クエリへのインデックス追加      |
| 9-07-c | EXPLAIN ANALYZE による実測検証（ステージングデータで確認） |


---

### 9-08 APIサーバー側ページネーションの実装 ★★★ ✅ 対応済

**対応要件：** 8.2, 12.1  
**現状（初版）：** バックログ5-06のE2Eテストで「21件以上でページ分割」を確認する計画があるが、サーバー側のページネーションAPIが実装項目として独立していない。フロントのみのページングでは全件取得後に分割するだけで性能要件を満たせない。

> **対応結果（第5期 チームB+チームC2+チームA INF-08、2026-05-06）：** 全12エンドポイントに `?page=&limit=` パラメータによるページネーションを実装。レスポンスに `{ data: [...], meta: { total, page, pageSize } }` 形式を適用。フロントエンド `app.js` の全10 `refreshXxx()` 関数を `Array.isArray(resp) ? resp : (resp.data || [])` パターンに対応済み。


| #      | 実装内容                                                                    |
| ------ | ----------------------------------------------------------------------- |
| 9-08-a | 全一覧APIに `?page=&limit=` パラメータによるページネーション対応                              |
| 9-08-b | レスポンスに `meta.total` / `meta.page` / `meta.pageSize` を含める（1-01-f の仕様を実装） |
| 9-08-c | カーソルベースページネーション（大量データ対応）への将来的な移行方針の策定                                   |


---

### 9-09 番号自動採番の競合制御 ★★★ ✅ 対応済

**対応要件：** 7.2, 7.3, 7.5, 7.7  
**現状（初版）：** 各 `generate*Code` 関数はインメモリで実装済み。DB永続化後は同時リクエストで重複番号が発行されるリスクがある。

> **対応結果（第2〜3期）：** `server/repositories/sequenceCounterRepository.js` で採番テーブルへの排他制御を実装。シードデータ最大値からの採番開始（`initial = {}` パラメータ追加）も対応済み（T-01修正、2026-05-06）。各採番コード（quotation:8, order:6, purchaseOrder:6, invoice:6, receipt:1, payment:2, delivery:1）の初期値設定を `server/index.js` で管理。


| #      | 実装内容                                             |
| ------ | ------------------------------------------------ |
| 9-09-a | PostgreSQL SEQUENCE または採番テーブルへの排他制御を使った採番ロジックの実装 |
| 9-09-b | 採番失敗時のリトライ設計                                     |


---

### 9-10 DBトランザクション境界の設計と実装 ★★★ ✅ 対応済

**対応要件：** 3.1, 9.3  
**現状（初版）：** 複数テーブルをまたぐ業務操作（承認→ステータス変更＋履歴INSERT＋通知INSERT等）のトランザクション設計が未定義。部分更新による不整合リスクがある。

> **対応結果（第5期 チームB+チームC1+チームA INF-10、2026-05-06）：** `server/db/transaction.js` に `withTransaction()` ユーティリティを実装（Drizzle ORM `db.transaction()` 利用）。`approvalService.js`（承認・却下）・`invoiceService.js`（採番+Invoice保存・ステータス更新+監査ログ）・`receiptService.js`（採番+Receipt保存+InvoiceステータスUPDATE）に適用。対象4ファイル67件テスト全通過（rollback時のエラー伝播確認テスト含む）。M7-3達成。


| #      | 実装内容                                   |
| ------ | -------------------------------------- |
| 9-10-a | 承認操作（ステータス変更・承認履歴保存・通知生成）を1トランザクションで実行 |
| 9-10-b | 請求確定（ステータス変更・採番・監査ログ）を1トランザクションで実行     |
| 9-10-c | Drizzle ORM のトランザクションAPIを使った各業務操作の実装   |


---

### 9-11 通知バッチ処理のスケジューラ実装 ★★

**対応要件：** 7.11（N-04/N-05/N-06）  
**現状：** バックログ3-02-dに「バッチ or ポーリング」と記載されているが独立した基盤タスクとして定義されていない。


| #      | 実装内容                                          |
| ------ | --------------------------------------------- |
| 9-11-a | Node-cron または pg_cron によるスケジューラの選定・実装         |
| 9-11-b | N-04（滞留通知）・N-05（支払期日通知）・N-06（納品予定日通知）のバッチ処理実装 |
| 9-11-c | バッチの冪等性設計（二重実行しても通知が重複しない制御）                  |
| 9-11-d | バッチ失敗時のアラートとリトライ設計                            |


---

### 9-12 楽観的ロックの実装 ★★

**対応要件：** 9.3, 16.3  
**現状：** バックログ5-07では「将来対応のE2Eテスト」として扱われているが、実装自体はDB永続化後に即座に必要となる。


| #      | 実装内容                                               |
| ------ | -------------------------------------------------- |
| 9-12-a | 各伝票テーブルに `version` カラムを追加                          |
| 9-12-b | 更新時に `version` が一致しない場合は 409 Conflict を返すミドルウェアの実装 |
| 9-12-c | フロントエンドでの 409 受信時の「他のユーザが更新しました。再読み込みしてください」表示     |


---

### 9-13 CSV出力のストリーミング処理 ★★

**対応要件：** 12.1（CSV1万件処理）  
**現状：** バックログ4-02はCSV出力の「有効化」のみ。全件インメモリ展開によるヒープ不足リスクへの対処が未定義。


| #      | 実装内容                                             |
| ------ | ------------------------------------------------ |
| 9-13-a | Node.js Stream（Transform Stream）を使ったCSVの逐次書き出し実装 |
| 9-13-b | 1万件規模でのメモリ使用量・処理時間の実測検証                          |


---

### 9-14 APIバージョニング戦略の策定 ★★

**対応要件：** 11.2, 12.5  
**現状：** 1-01のエンドポイント設計に `/api/`* のみ。バージョン管理がない状態で外部連携を追加すると互換性破壊が起きる。


| #      | 実装内容                               |
| ------ | ---------------------------------- |
| 9-14-a | 全エンドポイントを `/api/v1/*` プレフィックスに統一   |
| 9-14-b | APIバージョニング方針の策定（URLパス方式 vs ヘッダー方式） |


---

### 9-15 セキュリティ自動スキャンのCI組み込み ★★

**対応要件：** 12.3  
**現状：** 第2章は既知の脆弱性修正を扱うが、今後の依存ライブラリ追加で混入する新規脆弱性を継続検出する仕組みがない。


| #      | 実装内容                                         |
| ------ | -------------------------------------------- |
| 9-15-a | `npm audit` のCIジョブ化（PRごとに自動実行、高リスク脆弱性でビルド失敗） |
| 9-15-b | Snyk または Dependabot による依存関係自動監視              |
| 9-15-c | SAST（Semgrep等）による静的解析のCI組み込み                 |


---

### 9-16 OpenAPI仕様書の自動生成・公開 ★

**対応要件：** 11.2, 12.5  
**現状：** APIドキュメントの整備タスクが存在しない。


| #      | 実装内容                                  |
| ------ | ------------------------------------- |
| 9-16-a | `@fastify/swagger` によるOpenAPI仕様書の自動生成 |
| 9-16-b | Swagger UI の開発環境での公開（本番は認証保護）         |


---

### 9-17 フロントエンドのモジュール分割・バンドル最適化 ★

**対応要件：** 12.1, 5.2（モバイル対応）  
**現状：** app.js 単一ファイルに全画面ロジックが集約。初期ロード時間が問題になる可能性がある。


| #      | 実装内容                               |
| ------ | ---------------------------------- |
| 9-17-a | Vite によるルート単位のコードスプリッティング（遅延ロード）設定 |
| 9-17-b | バンドルサイズ・初期ロード時間の計測と最適化             |


---

## 10. UX基盤・画面品質（チームレビューで追加）

### 10-01 全画面フィードバックUI（ローディング・トースト・エラー表示） ★★★

**対応要件：** 全般（API接続後に全画面で必要）  
**現状：** インメモリ動作のため問題が顕在化していないが、API接続完了後に全画面で必要となる。


| #       | 実装内容                                    |
| ------- | --------------------------------------- |
| 10-01-a | API呼び出し中のローディングスピナー表示（ボタン非活性化を含む）       |
| 10-01-b | 保存成功・承認完了・送信完了時のトースト通知（自動消去）            |
| 10-01-c | APIエラー時のエラーメッセージ表示（エラーコード・詳細メッセージ）      |
| 10-01-d | ネットワークエラー時のリトライ誘導UI                     |
| 10-01-e | 全画面で共通のエラーハンドリングラッパー実装（`apiFetch` の共通化） |


---

### 10-02 破壊的操作の確認ダイアログ統一実装 ★★

**対応要件：** 6.5, 7.3, 7.11  
**現状：** 取消・却下・削除など取り消し不能な操作に統一されたモーダルダイアログが存在しない。


| #       | 実装内容                                   |
| ------- | -------------------------------------- |
| 10-02-a | 見積・受注・発注・請求・支払依頼の「取消」操作前の確認モーダル        |
| 10-02-b | 却下操作前の確認モーダル（コメント入力フォームを内包）            |
| 10-02-c | マスタの「無効化」操作前の確認モーダル                    |
| 10-02-d | `window.confirm()` の廃止とデザイン統一ダイアログへの統一 |


---

### 10-03 承認コメントの入力制御UI（却下：必須 / 承認：任意） ★★

**対応要件：** 7.11, 8.2.12  
**現状：** 実装計画上は定義されているが、コメント入力フォームの必須/任意制御UIの実装が未確認。


| #       | 実装内容                                  |
| ------- | ------------------------------------- |
| 10-03-a | S-12承認一覧の「却下」ボタンクリック時にコメント入力欄をインライン展開 |
| 10-03-b | 却下コメントを未入力のまま送信しようとした場合のバリデーションエラー表示  |
| 10-03-c | 承認操作のコメント入力欄を任意表示（プレースホルダー「任意」）       |
| 10-03-d | 承認一覧から遷移した場合のみ「承認一覧に戻る」ボタンを表示         |


---

### 10-04 マスタサジェスト（インクリメンタルサーチ）の実装 ★★

**対応要件：** 3.1, 要件4（二重入力解消）  
**現状：** API接続後に商品・顧客・仕入先の手入力による誤入力・転記ミスが発生するリスクがある。


| #       | 実装内容                                                     |
| ------- | -------------------------------------------------------- |
| 10-04-a | 見積・受注・発注・請求の明細行入力で商品名をインクリメンタルサーチし、選択時に単価・税区分を自動セット      |
| 10-04-b | 案件・見積・受注・発注・請求の顧客入力フィールドで顧客名をサジェスト表示し、選択時に締日・支払サイトを自動セット |
| 10-04-c | 発注・支払依頼の仕入先入力フィールドで仕入先名をサジェスト表示                          |


---

### 10-05 見積→受注転記時の差異チェックUI ★★

**対応要件：** 6.1（4.2）  
**現状：** サーバー側バリデーション（8-04-b）の実装と対になるフロントエンドUI。


| #       | 実装内容                                      |
| ------- | ----------------------------------------- |
| 10-05-a | 受注承認依頼送信時に受注金額と紐付き見積金額を比較して不一致をエラー表示      |
| 10-05-b | 受注詳細画面で「紐付き見積との差異なし」「差異あり（金額不一致）」のステータス表示 |


---

### 10-06 前後工程へのワンクリック導線UI ★★

**対応要件：** 7.4, 8.2.5  
**現状：** 受注から発注・請求への導線が実装計画として明示されていない。


| #       | 実装内容                                        |
| ------- | ------------------------------------------- |
| 10-06-a | 受注詳細画面に「発注を起票する」ボタンを設置（受注情報を引き継いで発注登録画面へ遷移） |
| 10-06-b | 受注詳細画面に「請求対象化する」ボタンを設置                      |
| 10-06-c | 発注詳細画面に「支払依頼を作成する」ボタンを設置（納品済ステータスのとき）       |


---

### 10-07 入金消込の手動マッチングUI ★★

**対応要件：** 7.8  
**現状：** `src/receipt.js` に計算ロジックは実装済みだが、担当者が入金と請求を手動で対応付ける操作UIの設計・実装が未定義。


| #       | 実装内容                       |
| ------- | -------------------------- |
| 10-07-a | 入金登録画面で消込対象請求書を選択・配分する操作UI |
| 10-07-b | 消込後の残高（未収残高）をリアルタイム表示      |
| 10-07-c | 消込確定前のプレビュー表示（消込前後の残高比較）   |


---

### 10-08 パンくずリストの全画面実装 ★★

**対応要件：** 8.1（14画面定義）  
**現状：** 詳細画面の階層が深くなるにつれてユーザが現在位置を見失うリスクがある。


| #       | 実装内容                                             |
| ------- | ------------------------------------------------ |
| 10-08-a | 共通ヘッダーコンポーネントにパンくずリストエリアを追加                      |
| 10-08-b | 各画面でパンくずを定義（例：「案件一覧 > PRJ-00001 > 見積 QUO-00001」） |


---

### 10-09 検索結果ゼロ時の Empty State 表示 ★★

**対応要件：** 7.12  
**現状：** 絞り込み結果がゼロ件の場合、空のテーブルが表示されてシステム障害と誤認されるリスクがある。


| #       | 実装内容                                          |
| ------- | --------------------------------------------- |
| 10-09-a | 全一覧画面で検索結果ゼロ件時の案内メッセージ表示（「該当する〇〇が見つかりませんでした」） |
| 10-09-b | 初期表示（データが1件もない場合）の案内メッセージ                     |


---

### 10-10 バリデーションエラーのインライン表示統一 ★★

**対応要件：** 8.2.1  
**現状：** バックログ5-02（E2Eテスト）はバリデーションの挙動テストだが、エラー表示のUI統一実装が独立した項目として存在しない。


| #       | 実装内容                                             |
| ------- | ------------------------------------------------ |
| 10-10-a | 必須フィールドのエラーメッセージをフィールド直下にインライン表示（フォーム上部まとめ表示の廃止） |
| 10-10-b | エラー発生フィールドへの自動フォーカス移動                            |
| 10-10-c | フィールド値修正後のエラーメッセージのリアルタイムクリア                     |


---

### 10-11 承認済み見積の顧客提出可否フラグと表示制御 ★★

**対応要件：** 7.3, 6.2.2  
**現状：** 「承認済みになるまで顧客提出可否を制御できること」が要件定義にあるが、フロントエンドの実装項目として存在しない。


| #       | 実装内容                           |
| ------- | ------------------------------ |
| 10-11-a | 見積詳細画面で承認前は「顧客提出不可（未承認）」バッジを表示 |
| 10-11-b | 承認済み状態でのみPDFダウンロードボタンを活性化      |
| 10-11-c | 承認状況エリアに「顧客提出可否ステータス」を明示       |


---

### 10-12 モバイルブラウザ対応（レスポンシブデザイン） ★★

**対応要件：** 5.2  
**現状：** バックログ全体を通じてモバイル対応タスクが一切存在しない。


| #       | 実装内容                                 |
| ------- | ------------------------------------ |
| 10-12-a | styles.cssへのメディアクエリ追加（ブレークポイント定義）    |
| 10-12-b | ナビゲーションメニューのモバイル用ハンバーガーメニュー化         |
| 10-12-c | 一覧テーブルのモバイル向け横スクロール対応                |
| 10-12-d | フォーム入力フィールドのタッチターゲットサイズ確保（44px以上）    |
| 10-12-e | 数値入力フィールドへの `inputmode="decimal"` 設定 |


---

### 10-13 帳票出力・CSV出力の権限制御UI ★★

**対応要件：** 10.3, 8.2  
**現状：** 帳票出力（4-01）・CSV出力（4-02）バックログに権限制御UIの実装が記述されていない。


| #       | 実装内容                              |
| ------- | --------------------------------- |
| 10-13-a | 出力権限のないユーザには帳票PDFボタン・CSV出力ボタンを非表示 |
| 10-13-b | レポート閲覧権限のないユーザにはS-13への遷移を制限       |


---

### 10-14 全一覧画面の共通UX標準の完全実装 ★★

**対応要件：** 8.2（全一覧画面共通要件）  
**現状：** API接続後に各画面で改めて実装・確認が必要だが、横断的な実装タスクとして定義されていない。


| #       | 実装内容                                   |
| ------- | -------------------------------------- |
| 10-14-a | 全一覧画面でのソート（列ヘッダークリック）の動作確認・修正          |
| 10-14-b | 全一覧画面での絞り込み条件がAPIのクエリパラメータとして機能することの確認 |
| 10-14-c | 権限に応じた操作ボタン（作成・編集・承認・出力）の表示制御を全画面で確認   |
| 10-14-d | ダッシュボードの各指標からフィルター条件付きで対象一覧画面へ遷移する実装   |


---

## 11. テスト追加（チームレビューで追加）

### 11-01 完全業務フロー E2E（見積→受注→発注→納品→請求→入金→支払） ★★★ ✅ 対応済

**対応要件：** 16.1  
**現状（初版）：** 部分フローのE2Eはあるが、1本通しのシナリオが存在しない。

> **対応結果（第4期 QA-H RT-01、2026-05-06）：** `e2e/full-flow.spec.js` を新規作成。見積→受注→発注・納品→請求→入金消込の全ステップを5 Step・22テストでカバー。フレイキー対策として `login()` 関数にサイドバー表示待機（`waitForSelector('[data-route="quotation"]', timeout:15000)`）を追加。22件全通過確認。


| #       | シナリオ                                                                              |
| ------- | --------------------------------------------------------------------------------- |
| 11-01-a | 見積作成・承認依頼→営業部長承認→受注作成（見積紐付け・添付）→受注承認→発注起票→発注承認→納品登録→検収済→請求起票→請求承認→請求書送付→入金登録→消込完了 |
| 11-01-b | 上記フローで各伝票の採番・ステータス・金額・紐付きの整合性を確認                                                  |


---

### 11-02 社長決裁ルートを含む完全承認フロー E2E ★★★ ✅ 対応済

**対応要件：** 6.1（3.2）  
**現状（初版）：** 社長決裁条件のロジックはあるが、2ステップ承認の完全フローE2Eが存在しない。

> **対応結果（第4期 QA-H RT-02、2026-05-06）：** `e2e/president-approval.spec.js` を新規作成。承認条件設定・2ステップ承認フロー・閾値境界値テストをカバー（16テスト）。16件全通過確認。11-02-a（社長決裁条件を満たす見積の完全承認フロー）・11-02-b（閾値ちょうど・1円超・1円未満の境界値確認）の両シナリオをカバー済み。


| #       | シナリオ                                            |
| ------- | ----------------------------------------------- |
| 11-02-a | 社長決裁条件を満たす見積（高額または低利益率）を作成→営業部長承認→社長承認→見積書PDF出力 |
| 11-02-b | 社長決裁条件の閾値ちょうど・1円超・1円未満でのルート分岐の境界値確認             |


---

### 11-03 入金消込の異常系 E2E ★★★ ✅ 対応済

**対応要件：** 7.8, 6.5, 16.1  
**現状（初版）：** Happy path（全額消込）のみ。実業務で頻発する異常系が未テスト。

> **対応結果（第4期 QA-I RT-03、2026-05-06）：** `e2e/receipt.spec.js` に入金消込異常系シナリオ9件を実装。一部入金・過入金・複数回部分消込・手数料差引入金の消込処理をカバー。9/9件全通過確認。


| #       | シナリオ                                           |
| ------- | ---------------------------------------------- |
| 11-03-a | 一部入金（請求100万円に対して60万円入金）→残40万円が未収残高に計上されること     |
| 11-03-b | 過入金（請求100万円に対して101万円入金）→差額1万円が「差額あり」状態で記録されること |
| 11-03-c | 複数回に分けた部分消込の累積残高計算の正確性確認                       |
| 11-03-d | 手数料差引入金（100万円請求、振込手数料550円差引で999,450円入金）の消込処理   |


---

### 11-04 受注承認依頼のバリデーション E2E ★★★ ✅ 対応済

**対応要件：** 6.1（4.1〜4.3）  
**現状（初版）：** 受注承認の基本フローE2Eはあるが、バリデーション失敗ケースが未テスト。

> **対応結果（第4期 QA-I RT-04、2026-05-06）：** `e2e/order-approval-validation.spec.js` を実装。見積未紐付け・金額不一致・添付なし→エラーブロック・下書き保存は添付なしでも成功の4シナリオをカバー。5/5件全通過確認（`page.once('dialog', ...)` パターンでデッドロック回避）。


| #       | シナリオ                                |
| ------- | ----------------------------------- |
| 11-04-a | 見積未紐付けのまま受注承認依頼→エラーでブロックされること       |
| 11-04-b | 受注金額と見積金額が不一致の状態で承認依頼→エラーでブロックされること |
| 11-04-c | 添付ファイルなしで承認依頼→エラーでブロックされること         |
| 11-04-d | 下書き保存は添付なしでも成功すること                  |


---

### 11-05 伝票状態遷移の制御 E2E ★★★ ✅ 対応済

**対応要件：** 3.1, 7.7, 13.2  
**現状（初版）：** 正常遷移のE2Eはあるが、不正遷移・変更制御の確認がない。

> **対応結果（第5〜6期 QA-E RT-05、2026-05-06）：** `e2e/quotation.spec.js` に取消済み見積の状態制御（3件）・`e2e/invoice.spec.js` に確定済み請求の状態制御（4件）を追加。合計7件全通過確認（BUG-01/02/03修正後に実行）。M10-2達成。


| #       | シナリオ                                        |
| ------- | ------------------------------------------- |
| 11-05-a | 確定済み請求を直接編集しようとしたときにブロックされること               |
| 11-05-b | 承認フローを経ずに見積を「承認済み」ステータスに変更するAPI呼び出しが拒否されること |
| 11-05-c | 取消済み見積から受注を作成しようとしたときにブロックされること             |
| 11-05-d | 取消済み受注を請求対象化しようとしたときにブロックされること              |


---

### 11-06 UI層の権限ネガティブテスト ★★★ ✅ 対応済

**対応要件：** 8.2, 10.5  
**現状（初版）：** バックログ5-05はAPIレベル（403確認）のみ。画面ボタン非表示のUI層テストがない。

> **対応結果（第4期 QA-I RT-06、2026-05-06）：** `e2e/permission-negative.spec.js` を実装。承認権限なし・作成権限なし・出力権限なし・マスタ管理権限なしの4権限×複数画面を10テストでカバー。10/10件全通過確認。テスト用ユーザ（`viewer`・`approver-readonly`）を `app.js` の `users` 配列に追加済み。


| #       | シナリオ                                             |
| ------- | ------------------------------------------------ |
| 11-06-a | 承認権限のないユーザがログインしたとき、各伝票詳細に「承認する」「却下」ボタンが表示されないこと |
| 11-06-b | 作成権限のないユーザがログインしたとき、各一覧画面に「新規作成」ボタンが表示されないこと     |
| 11-06-c | 出力権限のないユーザがログインしたとき、PDF・CSV出力ボタンが表示されないこと        |
| 11-06-d | マスタ管理権限のないユーザがS-11（マスタ管理）にアクセスできないこと             |


---

### 11-07 CI/CDパイプラインの自動テスト実行 ★★★ ✅ 対応済

**対応要件：** CLAUDE.md（TDD・カバレッジ要件）  
**現状（初版）：** テストコードはあるが、自動実行の仕組みがない。

> **対応結果（第4期 SRE-I INF-01 / RT-07、2026-05-06）：** `.github/workflows/ci.yml` に unit-test（vitest + 行カバレッジ80%閾値チェック）・e2e-test（Playwright Chromium + PostgreSQL service container）・deploy-staging（main ブランチのみ）の3ジョブを実装。`package.json` に `test:coverage` スクリプト追加済み。11-07-a〜c 全対応済み。


| #       | 実装内容                                          |
| ------- | --------------------------------------------- |
| 11-07-a | PRトリガーでVitest（ユニットテスト）を自動実行するCIジョブ            |
| 11-07-b | カバレッジを自動計測し、80%未満のPRをブロックするCIジョブ（CLAUDE.md要件） |
| 11-07-c | マージ後にPlaywright（E2Eテスト）を自動実行するCIジョブ           |


---

### 11-08 一部納品・検収NGの発注残管理 E2E ★★

**対応要件：** 7.5, 7.6  
**現状：** 5-04-aに「一部納品後の発注残数量変化の確認」はあるが、検収NGシナリオが含まれていない。


| #       | シナリオ                                              |
| ------- | ------------------------------------------------- |
| 11-08-a | 発注数量10のうち7個が納品・検収済、3個が検収NG→発注残が3（NG分を継続計上）であること   |
| 11-08-b | 全明細検収完了後（最後の1明細が検収済になった瞬間）に発注ステータスが「納品済」に自動遷移すること |


---

### 11-09 複数受注の合算請求における消費税計算テスト ★★

**対応要件：** 7.7  
**現状：** 消費税の一括計算ロジックの単体テスト・E2Eが未定義。


| #       | 実装内容                                                 |
| ------- | ---------------------------------------------------- |
| 11-09-a | 2件の受注（それぞれ税込・税抜明細混在）を合算した請求の消費税が「課税合計への一括計算」で算出されること |
| 11-09-b | 明細ごとの税額合計と一括計算の差異が端数処理で生じないことの確認                     |


---

### 11-10 データ整合性テスト（状態・集計） ★★

**対応要件：** 16.3  
**現状：** 個別機能のテストはあるが、案件・受注・請求・入金を横断した整合性テストがない。


| #       | 実装内容                                                  |
| ------- | ----------------------------------------------------- |
| 11-10-a | 案件に複数の受注・請求・入金が紐付いた状態での売上・原価・粗利・未収・未払の集計値が正確であること     |
| 11-10-b | 無効化された顧客マスタを参照する既存見積が表示継続されること、かつ新規見積の顧客選択候補に表示されないこと |
| 11-10-c | 見積改版後に旧版のデータが変更されていないこと、最新版フラグが正しく切り替わること             |


---

### 11-11 権限変更後の反映テスト ★★

**対応要件：** 10.5  
**現状：** 権限変更が次回ログイン時に反映されることのテストが存在しない。


| #       | シナリオ                               |
| ------- | ---------------------------------- |
| 11-11-a | 承認権限を付与→ログアウト→再ログイン→承認ボタンが表示されること  |
| 11-11-b | 承認権限を削除→ログアウト→再ログイン→承認ボタンが非表示であること |


---

### 11-12 性能テスト ★★

**対応要件：** 12.1  
**現状：** 5-06（ページネーションE2E）は件数確認のみで、応答時間の計測テストが存在しない。


| #       | 実装内容                                                 |
| ------- | ---------------------------------------------------- |
| 11-12-a | 一覧画面（見積・受注・発注・請求）の3秒以内表示をk6 / Artilleryで計測（p95レイテンシ） |
| 11-12-b | CSV出力（1万件）の処理時間計測と許容値（例：30秒以内）へのアサーション               |
| 11-12-c | 負荷テスト（同時接続ユーザ数×リクエスト）によるスループット確認                     |


---

### 11-13 DBマイグレーション整合性テスト ★★

**対応要件：** 9.3  
**現状：** 1-02-bにDrizzle ORM のマイグレーション管理はあるが、マイグレーション自体のテストが未定義。


| #       | 実装内容                                          |
| ------- | --------------------------------------------- |
| 11-13-a | 全マイグレーションファイルを順に適用した結果のスキーマが期待通りであることを確認するテスト |
| 11-13-b | ロールバック操作のテスト（直前のマイグレーションを取り消して整合性が保たれること）     |


---

## 優先度別サマリ

> チームレビュー（2026-05-06）反映済み。8〜11章を含む全バックログを対象としている。

### ★★★ 緊急（本番稼働の前提条件）


| 項目                                    | カテゴリ      | 状態                         |
| ------------------------------------- | --------- | -------------------------- |
| ✅ 1-01 バックエンドAPI実装                    | 基盤        | 対応済（第2〜3期）                 |
| ✅ 1-02 データベース永続化                      | 基盤        | 対応済（第2〜3期）                 |
| ✅ 1-03 認証基盤の刷新（JWT + bcrypt）          | 基盤・セキュリティ | 対応済（第2〜4期）                 |
| ✅ 1-04 操作ログ・監査証跡                      | 基盤        | 主要操作対応済（第3〜5期）1-04-e/f 未着手 |
| ✅ 2-01 サーバー側認可ミドルウェア                  | セキュリティ    | 対応済（第3〜4期）                 |
| ✅ 2-02 JWT秘密鍵管理                       | セキュリティ    | 対応済（第4期 SRE-I）             |
| 5-01 却下→修正→再申請 E2E                    | テスト       | **未着手**                    |
| ✅ 5-02 各画面バリデーション E2E の分散             | テスト       | 対応済（第6期 P10-RT-02）         |
| △ 5-05 サーバー側認可APIテスト                  | テスト       | 一部対応済（AT-01: 5-05-e/f のみ）  |
| 8-01 発注の仕入先別分割                        | 業務ロジック    | **未着手**                    |
| 8-02 合算請求と消費税一括計算                     | 業務ロジック    | **未着手**                    |
| 8-03 顧客マスタの締日・支払サイト管理                 | 業務ロジック    | **未着手**                    |
| 8-04 受注承認依頼のサーバー側バリデーション              | 業務ロジック    | **未着手**                    |
| ✅ 9-01 DBバックアップ・障害復旧設計                | インフラ      | 対応済（第4期 INF-02）            |
| ✅ 9-02 HTTPS/TLS設定と証明書管理              | インフラ      | 対応済（第4期 INF-03）            |
| △ 9-03 アプリケーションログ収集・集約基盤              | インフラ      | 9-03-a のみ対応済（第4期 SRE-I）    |
| 9-04 ヘルスチェック・死活監視・アラート                | インフラ      | **未着手**                    |
| ✅ 9-05 CI/CDパイプライン構築                  | インフラ      | 対応済（第4期 INF-01）            |
| ✅ 9-06 環境分離（開発・ステージング・本番）             | インフラ      | 対応済（第4期 INF-06）            |
| ✅ 9-07 DBインデックス設計・クエリ最適化              | インフラ      | 対応済（第5期 INF-07）            |
| ✅ 9-08 APIサーバー側ページネーションの実装            | インフラ      | 対応済（第5期 INF-08）            |
| ✅ 9-09 番号自動採番の競合制御                    | インフラ      | 対応済（第2〜3期）                 |
| ✅ 9-10 DBトランザクション境界の設計と実装             | インフラ      | 対応済（第5期 INF-10）            |
| 10-01 全画面フィードバックUI（ローディング・トースト・エラー表示） | UX        | **未着手**                    |
| ✅ 11-01 完全業務フロー E2E                   | テスト       | 対応済（第4期 RT-01）             |
| ✅ 11-02 社長決裁ルートを含む完全承認フロー E2E         | テスト       | 対応済（第4期 RT-02）             |
| ✅ 11-03 入金消込の異常系 E2E                  | テスト       | 対応済（第4期 RT-03）             |
| ✅ 11-04 受注承認依頼のバリデーション E2E            | テスト       | 対応済（第4期 RT-04）             |
| ✅ 11-05 伝票状態遷移の制御 E2E                 | テスト       | 対応済（第5〜6期 RT-05）           |
| ✅ 11-06 UI層の権限ネガティブテスト                | テスト       | 対応済（第4期 RT-06）             |
| ✅ 11-07 CI/CDパイプラインの自動テスト実行           | テスト       | 対応済（第4期 INF-01）            |


### ★★ 重要（リリース品質に影響）


| 項目                             | カテゴリ    | 状態                               |
| ------------------------------ | ------- | -------------------------------- |
| 2-03 通知の所有者チェック                | セキュリティ  | **未着手**                          |
| 2-04 Mass Assignment 対策        | セキュリティ  | **未着手**                          |
| ✅ 2-05 ログイン総当たり防御              | セキュリティ  | 対応済（第4期 P9-B-03）                 |
| ✅ 2-06 CSRF 対策                 | セキュリティ  | 対応済（第3〜4期）                       |
| 3-01 S-07 納品・検収の独立画面化          | フロントエンド | **未着手**                          |
| 3-02 S-14 通知ロジックの完全実装          | フロントエンド | **未着手**                          |
| 3-03 S-15 承認条件設定UI             | フロントエンド | **未着手**                          |
| 3-04 見積の複製・改版・版管理              | フロントエンド | **未着手**                          |
| 3-05 添付ファイル管理                  | フロントエンド | **未着手**                          |
| 3-06 承認状況エリアの完全実装              | フロントエンド | **未着手**                          |
| 3-07 複数ステップ承認フロー               | フロントエンド | **未着手**                          |
| 4-01 帳票PDF出力（全帳票）              | 業務ロジック  | **未着手**                          |
| 4-02 CSV出力の有効化                 | 業務ロジック  | **未着手**                          |
| 4-03 月次締め処理                    | 業務ロジック  | **未着手**                          |
| 4-04 失注・取消の記録                  | 業務ロジック  | **未着手**                          |
| 4-05 請求の訂正・取消処理                | 業務ロジック  | **未着手**                          |
| ✅ 5-03 複数ステップ承認の途中却下 E2E       | テスト     | 対応済（第4期 P10-RT-03）               |
| ✅ 5-04 発注→納品→請求データ連鎖 E2E       | テスト     | 対応済（第6期 P10-RT-04）               |
| 8-05 顧客担当者マスタ                  | 業務ロジック  | **未着手**                          |
| 8-06 部署・役職マスタの管理画面             | 業務ロジック  | **未着手**                          |
| 8-07 仕入先銀行口座情報                 | 業務ロジック  | **未着手**                          |
| 8-08 過入金・調整伝票の処理               | 業務ロジック  | **未着手**                          |
| 8-09 受注の「契約手続き済」ステータス管理        | 業務ロジック  | **未着手**                          |
| △ 8-10 アカウントロックとパスワード変更要求      | 業務ロジック  | 8-10-a/b 対応済（第4期 T-10）8-10-c 未着手 |
| 8-11 案件詳細画面の財務集計表示             | 業務ロジック  | **未着手**                          |
| 8-12 S-13レポート画面の絞り込み条件UI       | 業務ロジック  | **未着手**                          |
| 9-11 通知バッチ処理のスケジューラ実装          | インフラ    | **未着手**                          |
| 9-12 楽観的ロックの実装                 | インフラ    | **未着手**                          |
| 9-13 CSV出力のストリーミング処理           | インフラ    | **未着手**                          |
| 9-14 APIバージョニング戦略の策定           | インフラ    | **未着手**                          |
| 9-15 セキュリティ自動スキャンのCI組み込み       | インフラ    | **未着手**                          |
| 10-02 破壊的操作の確認ダイアログ統一実装        | UX      | **未着手**                          |
| 10-03 承認コメントの入力制御UI            | UX      | **未着手**                          |
| 10-04 マスタサジェスト（インクリメンタルサーチ）の実装 | UX      | **未着手**                          |
| 10-05 見積→受注転記時の差異チェックUI        | UX      | **未着手**                          |
| 10-06 前後工程へのワンクリック導線UI         | UX      | **未着手**                          |
| 10-07 入金消込の手動マッチングUI           | UX      | **未着手**                          |
| 10-08 パンくずリストの全画面実装            | UX      | **未着手**                          |
| 10-09 検索結果ゼロ時の Empty State 表示  | UX      |                                  |
| 10-10 バリデーションエラーのインライン表示統一     | UX      |                                  |
| 10-11 承認済み見積の顧客提出可否フラグと表示制御    | UX      |                                  |
| 10-12 モバイルブラウザ対応（レスポンシブデザイン）   | UX      |                                  |
| 10-13 帳票出力・CSV出力の権限制御UI        | UX      |                                  |
| 10-14 全一覧画面の共通UX標準の完全実装        | UX      |                                  |
| 11-08 一部納品・検収NGの発注残管理 E2E      | テスト     |                                  |
| 11-09 複数受注の合算請求における消費税計算テスト    | テスト     |                                  |
| 11-10 データ整合性テスト（状態・集計）         | テスト     |                                  |
| 11-11 権限変更後の反映テスト              | テスト     |                                  |
| 11-12 性能テスト                    | テスト     |                                  |
| 11-13 DBマイグレーション整合性テスト         | テスト     |                                  |


### ★ 通常（機能完全性）


| 項目                           | カテゴリ       | 状態               |
| ---------------------------- | ---------- | ---------------- |
| ✅ 2-07 JWT失効・セッション管理         | セキュリティ     | 対応済（第4期 T-11）    |
| ✅ 2-08 XSS 対策・CSP            | セキュリティ     | 対応済（第4期 P9-E-BE） |
| 3-08 ダッシュボードの権限別表示制御         | フロントエンド    | **未着手**          |
| 4-06 発注数量変更の変更履歴             | 業務ロジック     | **未着手**          |
| 4-07 ユーザ個別権限の設定UI            | 業務ロジック     | **未着手**          |
| 5-06 大量データ・ページネーション E2E      | テスト        | **未着手**          |
| 7-01〜7-06 初期データ移行            | 移行         | **未着手**          |
| 8-13 マスタ種別ごとの変更履歴確認機能        | 業務ロジック     | **未着手**          |
| **8-14 レポート画面のグラフ表示機能**      | **業務ロジック** | **未着手**          |
| 9-16 OpenAPI仕様書の自動生成・公開      | インフラ       | **未着手**          |
| 9-17 フロントエンドのモジュール分割・バンドル最適化 | インフラ       | **未着手**          |


### ☆ 将来対応


| 項目                                 | カテゴリ        | 状態                  |
| ---------------------------------- | ----------- | ------------------- |
| ~~2-07-d/e JWT リフレッシュトークンローテーション~~ | ~~セキュリティ~~  | ✅ T-11で対応済（2-07に統合） |
| **3-09 ダッシュボードのユーザカスタマイズ**         | **フロントエンド** | 将来対応                |
| 5-07 同時操作・競合シナリオ                   | テスト         | 将来対応                |
| 6-01〜6-06 外部システム連携                 | 連携          | 将来対応                |


---

*本バックログは実装計画の改版に伴い随時更新する。*