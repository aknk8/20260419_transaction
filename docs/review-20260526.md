調査結果です。直近CI run `26390121820` は `Unit Tests & Coverage` は成功、`E2E Tests (Playwright)` が失敗しています。E2Eは `692` 件中 `423 failed / 269 passed` でした。

主因はアプリ実装ではなく、E2Eの期待値・モックが現行実装とずれていることです。

1つ目は [e2e/customer-master.spec.js](C:/Users/anaka/OneDrive/ドキュメント/GitHub/20260419_transaction/e2e/customer-master.spec.js:6) です。ここで `GET /api/**` を `route.abort()` し、POST/PATCHは空 `{}` で成功扱いにしています。一方、実装は顧客登録/更新後に [app.js](C:/Users/anaka/OneDrive/ドキュメント/GitHub/20260419_transaction/app.js:6208) の `refreshCustomers()` でAPIから再取得します。GETがabortされるため一覧は更新されず、CI先頭の2件と同じ失敗をローカルでも再現しました。

2つ目は [e2e/dashboard.spec.js](C:/Users/anaka/OneDrive/ドキュメント/GitHub/20260419_transaction/e2e/dashboard.spec.js:58) です。テストは旧期待値 `承認待ち=02`, `未収=01` と `.narrow-panel` に `QUO-00003` / `POD-00006` がある前提ですが、現行画面では `承認待ち=04`, `未収=02` で、[app.js](C:/Users/anaka/OneDrive/ドキュメント/GitHub/20260419_transaction/app.js:2132) の `.narrow-panel` は「部門別売上」パネルです。これもローカル単体でCIと同じ4件が再現しました。

加えて、[playwright.config.js](C:/Users/anaka/OneDrive/ドキュメント/GitHub/20260419_transaction/playwright.config.js:8) は `workers: 4` で、固定コードのデータを更新するE2Eが同じDBを共有しています。終盤の大量タイムアウト、例えば `.sidebar [data-route="payment"]` や `.sidebar [data-route="master"]` 待ち続けは、この共有状態・固定データ前提の崩れが連鎖している可能性が高いです。

確認したコマンド:
`gh run view 26390121820 --json ...`
`gh run view 26390121820 --log-failed`
`npx playwright test e2e/customer-master.spec.js --workers=1 --reporter=list --output=pw-out`
`npx playwright test e2e/dashboard.spec.js --workers=1 --reporter=list --output=pw-out-dashboard`

次に直すなら、優先度は `customer-master` のAPIモック修正、`dashboard` の期待値/セレクタ更新、最後にE2EのDB分離または `workers: 1` 化です。